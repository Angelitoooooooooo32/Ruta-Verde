version: '3.8'

services:
  ruta-verde:
    build: .
    container_name: ruta-verde-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - ruta-verde-network

networks:
  ruta-verde-network:
    driver: bridge
